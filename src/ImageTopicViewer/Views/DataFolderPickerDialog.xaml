<Window x:Class="ImageTopicViewer.Views.DataFolderPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="데이터 폴더 선택"
        Width="480" Height="220"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                    Text="이미지를 저장할 폴더를 선택해주세요."
                    FontWeight="Bold" FontSize="14" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1"
                    Text="선택한 폴더에 대주제/소주제별로 이미지가 저장됩니다. 나중에 설정에서 변경할 수 있습니다."
                    TextWrapping="Wrap" Margin="0,0,0,12"/>

        <Grid Grid.Row="2" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SelectedPathTextBox" Grid.Column="0"
                     IsReadOnly="True" VerticalContentAlignment="Center"
                     Padding="4" Margin="0,0,8,0"/>
            <Button x:Name="BrowseButton" Grid.Column="1"
                    Content="찾아보기..." Padding="8,4" Click="BrowseButton_Click"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="OkButton" Content="확인" Width="80" Margin="0,0,8,0"
                    IsDefault="True" IsEnabled="False" Click="OkButton_Click"/>
            <Button Content="취소" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
